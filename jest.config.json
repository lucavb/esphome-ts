{
    "preset": "ts-jest",
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": ["<rootDir>/src/api/protobuf", "<rootDir>/test"],
    "reporters": ["default", ["jest-junit", { "outputDirectory": ".", "outputName": "junit.xml" }]]
}
